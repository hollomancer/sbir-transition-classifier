# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd

# C extensions
*.so

# Distribution / packaging
.Python
build/
dist/
downloads/
eggs/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
wheels/

# pyenv / virtualenv
.python-version
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Editor / IDE
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.swp
*.swo
*~
*.bak

# macOS
.DS_Store
.DS_Store?

# Test / coverage / caches
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.coverage
coverage.xml
htmlcov/
.cache/
.hypothesis/

# Environments / secrets
.env
.env.*
secrets.*
*.pem
*.key

# Logs
*.log
logs/
log/

# SQLite and database artifacts (do not commit DBs)
*.db
*.sqlite
*.sqlite3
*.sqlite3-journal

# Large data files and directories (keep big inputs out of repo)
# If small fixtures are needed for tests, move them to tests/fixtures and keep lightweight.
data/
data_subset/
test_data/
test_results/
output/
reports/

# Local configuration overrides
config/local.yaml
config/custom.yaml
config/*.local.yaml

# Documentation build outputs
/site
docs/build/
docs/.cache/
site/

# Tooling / local caches
.gemini/
.vfs/
tmp/
temp/
*.tmp

# OS / tooling artifacts
Thumbs.db
ehthumbs.db

# Project-specific generated files to ignore (top-level only)
# Use leading slash to scope these ignores to the repository root so fixtures
# placed under tests/fixtures/ can be committed.
#/ - indicates top-level path
/test_detections.jsonl
/test_detections_summary.csv
/sbir_classifier.db
/sbir_transitions.db

# Safety: ignore any accidental binary or large files
*.zip
*.tar.gz
*.tgz
*.rar
*.7z
*.exe
*.dll
*.so

# Python packaging lockfiles (if using Poetry)
poetry.lock

# Misc
coverage/
